<van-sticky>

  <view>設置步驟條 - 待定 - 待完成</view>
  
  <view class="action">
    <button class="cu-btn bg-green shadow" bindtap="numSteps">下一步</button>
  </view>
  
  <view class="cu-steps">
    <view class="cu-item {{index>num?'':'text-blue'}}" wx:for="{{numList}}" wx:key>
      <text class="num {{index==2?'err':''}}" data-index="{{index + 1}}"></text> {{item.name}}
    </view>
  </view>

</van-sticky>


<!-- 課程信息錄入 -->

<!-- 不同格子代表不同課程信息 -->
<!-- <view class="grid col-3 padding-sm">
  <view class="padding-sm" wx:for="{{ColorList}}" wx:key="title">
    <view class="bg-{{item.name}} padding radius text-center shadow-blur">
      <view class="text-lg">{{item.title}}</view>
      <view class="margin-top-sm text-Abc">{{item.name}}</view>
    </view>
  </view>
</view> -->


<view class="grid col-{{4}} padding-sm">
  <view class="padding-sm" wx:for="{{courseNameArray}}" wx:key="id">
    <view class="bg-{{activeArray[index] ? ColorList[index].name : 'yellow'}} padding radius text-center shadow-blur " data-index="{{index}}" bindtap="BindAddInfo">
        <text class="cuIcon-unfold text-sl" /> 
        <view class="text-lg">{{item}}</view>
      <view class="margin-top-sm text-df">點我添加</view>
    </view>
  </view>
</view>



<!-- for循環渲染方法 - 優勢：渲染方便；劣勢：不美觀 - 未完成 -->
<van-cell-group
wx:for="{{courseInfoInput}}"
wx:key="id"
wx:if="{{InfoDisplay[index]}}"
>
  <van-cell title-width="250rpx" center size="large" title="{{item.name}}" data-cellIndex="{{index}}" bind:click="bindCellClick" >
    <view>{{item.input}}</view>
  </van-cell>
</van-cell-group>


